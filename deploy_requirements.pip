# Picky aims to be a pure python website. However, I like to use
# Fabric for deploys, which has C library dependencies. So we
# separate those dependencies into this file.

paramiko==2.4.1
pycrypto==2.6.1
Fabric==2.2.2
